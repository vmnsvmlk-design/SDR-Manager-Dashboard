{
  "name": "sdr-manager-dashboard-local",
  "version": "1.0.0",
  "private": true,
  "description": "Local, terminal-run clone of the SDR Manager Dashboard - same UI and live HubSpot data as the Apps Script version, served from a local Node server instead.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
